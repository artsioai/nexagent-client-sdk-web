{
  "name": "@newcast/nexagent-sdk-web",
  "version": "0.0.2",
  "description": "Nexagent Web SDK",
  "keywords": [
    "Voice",
    "Agent",
    "SDK",
    "Nexagent"
  ],
  "homepage": "https://github.com/NexAgent/client-sdk-web#readme",
  "bugs": {
    "url": "https://github.com/NexAgent/client-sdk-web/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NexAgent/client-sdk-web.git"
  },
  "license": "MIT",
  "author": "nexagent",
  "type": "commonjs",
  "main": "dist/nexagent.js",
  "types": "dist/nexagent.d.ts",
  "directories": {
    "example": "example"
  },
  "scripts": {
    "test": "jest",
    "build": "tsc && tsc --declaration",
    "start": "npm run generate-api && npm run build && node dist/nexagent.js",
    "generate-api": "./generate-api.sh",
    "update": "./update.sh",
    "generate-local-build": "npm install && npm run build && npm pack",
    "test:example": "npm run pack:local && cd example && npm install && npm run build",
    "pack:local": "./scripts/build-latest.sh",
    "clean-builds": "rm -f nexagent-web-*.tgz && echo '🧹 Cleaned up all build tarballs'",
    "dev:example": "npm run pack:local && cd example && npm install && npm run dev",
    "re-build-local-example": "./scripts/re-build-local-example.sh"
  },
  "dependencies": {
    "@daily-co/daily-js": "^0.84.0",
    "events": "^3.3.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.0.4",
    "jest": "^30.0.3",
    "openai": "^5.7.0",
    "swagger-typescript-api": "^13.2.7",
    "ts-jest": "^29.4.0",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
